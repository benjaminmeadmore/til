# Check Ubuntu Version

Most of the Linux servers we operate use the Ubuntu distro. To determine which release the current distro is at run the following cmd:

```bash
$ lsb_release -r
```
The expected output should look like this: 

```bash
Release:	22.04
```